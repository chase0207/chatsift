# W21 技术方案 — 辅助采集器

> 版本: v1.2.0
> 日期: 2026-06-09
> 状态: Draft  
> 输入: `docs/prd/V1.0/W21_design.md` + `docs/research/2026-06-08_W21只读调研与PRD复审.md`  
> 边界: 本文仅为方案;W21 代码实现须等 W20 E1/E2 验收并合回 main 后启动

---

## 变更日志

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0.0 | 2026-06-08 | 初版:状态机、候选策略、人工互锁、W20 依赖和验收 |
| v1.1.0 | 2026-06-08 | 修正 P0:legacy 10s debounce 与 3s 缓冲冲突;候选 `dom_ref` 跨轮失效;拍板 Q1/Q2:采集权只读接口归 W20 域,heartbeat/block-warn 是 W20 交付依赖 |
| v1.2.0 | 2026-06-09 | 明确插件面板开关入口;同步 W20 账号级 block + 最早实例仲裁口径;移除 warn 依赖;把 collect-permission 约束为 W20 前置能力 |

## 1. 总体方案

新增插件运行时模块:

```text
plugin/runtime/assisted-collector.js
```

该模块只负责“低频选择候选会话并调用 adapter.switchSession()”。消息采集、去重、position、上报仍走现有 `legacy-collector + event-collector + event-uploader` 链路。

W21 不新增消息数据契约,不改 W17 position/message_id,不新增发送能力。

## 2. 模块关系

```text
插件面板“客服配置-自动化配置-自动切换会话”
        |
        v
RpaFeatureFlags + ChatsiftContentGate
        |
        v
AssistedCollector
        |
        +-- AdapterRegistry.resolve(location)
        +-- adapter.detectSessions()
        +-- adapter.switchSession(session)
        +-- adapter.confirmActiveSession(session)
        |
        v
LegacyCollector 现有采集链路
```

关键约束:不能假设“切换会话后 3 秒内自然采集完成”。当前 legacy collector 是 MutationObserver 触发 `_scheduleCollect()`,且 debounce 为 10 秒。W21 必须显式触发一次采集,或停留超过 debounce 窗口并确认采集 fire。

## 3. 状态机

```text
disabled
  -> idle
  -> paused_by_human
  -> scanning
  -> cooldown
  -> blocked_by_w20
```

状态含义:

| 状态 | 含义 |
|---|---|
| disabled | `auto_switch_session=false` 或非目标页面 |
| idle | 开关开启,但未满足无人工操作阈值 |
| paused_by_human | 检测到人工操作,暂停 |
| scanning | 正在处理候选会话 |
| cooldown | 单轮完成后的冷却期 |
| blocked_by_w20 | W20 heartbeat 返回 account block 或采集权不可用 |

## 4. 启动条件

必须同时满足:

- `auto_switch_session=true`
- `collector_v1_enabled=true`
- `ChatsiftContentGate.isAllowed()=true`
- 当前 adapter 为 `douyin/private-message`
- 当前页面能读到 `account_biz_id`
- W20 判定当前员工可采该账号
- 最近人工操作距今超过阈值,默认 15 分钟
- 当前不处于 W20 account block

任一条件不满足,不调用 `switchSession()`。

## 5. W20 采集权检查

拍板:采集权只读能力归 W20 域,W21 只消费,不在 W21 分支新开 server 采集权接口。

优先方案:复用 W20 heartbeat 返回的治理状态和采集权结果。

若 W20 heartbeat 不返回采集权状态,则由 W20 收口补一个轻量只读接口:

```text
GET /api/v1/service-accounts/collect-permission
query: platform, platform_page, account_biz_id
return:
{
  allowed: boolean,
  reason: "ok|pending_owner|not_collector|account_disabled|missing_account|unknown",
  lifecycle,
  collector_id,
  collector_kind
}
```

约束:

- 该接口由 W20 收口阶段提供契约和实现,或作为 W20.1 小任务补齐。
- W21 代码实现前必须确认该能力已在 main 可用。
- 只读接口,不建账号,不改数据。
- 首见建 pending 仍只发生在 events batch 入库路径。
- W21 不绕过 server 端 batch 的最终采集权闸门。
- 如果 W20 未提供 heartbeat payload 或只读接口,W21 代码阶段暂停,不在 W21 分支补 server 面。

依赖口径:W21 依赖 W20 merge 后的账号级实例冲突能力。当前执行口径为 `conflict=account/action=block`,最早注册实例继续,后注册实例暂停;不依赖会话级 block,不依赖 warn。

## 6. 候选会话策略

第一版只处理当前可见列表。

候选来源:

```js
sessions = await adapter.detectSessions()
candidates = sessions.filter(s => s.unread_count > 0 && s.dom_ref)
```

排序:

1. `unread_count` 降序。
2. DOM 列表顺序。

限制:

- 单轮最多 5 个。
- 两次切换间隔不得低于 12 秒,再叠加随机抖动。
- 单轮结束后冷却 3 分钟。
- 单账号每分钟最多 6 次自动切换。

候选不能在轮首一次性缓存 `dom_ref` 后连续使用。切换会话会导致列表重渲染、未读消失或排序变化,旧 `dom_ref` 可能 stale。W21 必须:

1. 轮首只生成候选计划,记录稳定候选键。
2. 每次切换前重新 `detectSessions()`。
3. 用稳定候选键重匹配新的 `dom_ref`。
4. 找不到则跳过该候选。

候选键优先级:

1. `raw_id`
2. `nickname + last_message_text`
3. `nickname`

`nickname` 兜底可能误匹配,只用于低风险补采;真机验收要记录误匹配情况。

## 7. 切换流程

单个候选:

```text
1. 再次检查人工互锁和 W20 block 状态
2. 重新 detectSessions(),按候选键重匹配当前 DOM 节点
3. adapter.switchSession(session)
4. adapter.confirmActiveSession(session)
5. 等 DOM 稳定 1.5s
6. 触发或等待采集完成
7. 写本地日志
```

采集完成策略:

| 方案 | 说明 | 结论 |
|---|---|---|
| A. 显式采集 | legacy collector 暴露 `collectNow()` 或复用 `collectMessageSession()` 包装,切换确认后立即采当前会话 | 推荐 |
| B. 等待 debounce | 每个候选停留至少 12 秒,覆盖 10 秒 debounce,并通过日志确认采集 fire | 备选 |

推荐 A。原因:仅靠停留时间仍受 DOM 持续变化影响,可能重置 debounce;显式采集更可控。若实现 A,legacy collector 需提供一个安全入口,但不得改变 position/message_id 规则。

失败处理:

| reason | 处理 |
|---|---|
| auto-switch-disabled | 退出到 disabled |
| no-dom-ref | 跳过候选 |
| switch-timeout | 记录失败,本轮不重试 |
| human-active | 进入 paused_by_human |
| w20-blocked | 进入 blocked_by_w20 |
| non-target-page | 进入 disabled |

## 8. 人工互锁

监听事件:

- `mousemove`
- `mousedown`
- `keydown`
- `wheel`
- `focus`

策略:

- 任一事件更新 `lastHumanActionAt`。
- `scanning` 中检测到人工事件,当前轮立即停止。
- 默认 15 分钟无人工操作后才可重新进入扫描。
- 用户手动关闭开关立即停止并清 timer。

## 9. W20 治理状态

W20 heartbeat 事件(待 W20 验收并 merge 后可用):

- `account block`:停止 assisted collector,不再切换会话;legacy collector 是否停采由 W20 逻辑负责。
- `not_collector` / `pending_grab` / `account_disabled`:不自动切换,仅记录状态和展示提示。
- 无冲突且采集权可用:允许恢复。

说明:W20 最新实例冲突口径已去掉 warn 和会话维度。W21 只消费账号级 block;同账号多实例时由 W20 仲裁“最早注册继续、更晚暂停”。

## 10. 构建接入

`plugin/build.js` 模块顺序建议:

```text
runtime/instance-identity.js
runtime/event-uploader.js
...
runtime/legacy-collector.js
runtime/assisted-collector.js
```

理由:

- assisted collector 依赖 adapter registry、feature flags、content gate。
- 它不应被 legacy collector 依赖。
- legacy collector 负责采集,assisted collector 只负责切换。

## 11. 不变量

必须保持:

- 不新增发送入口。
- 不调用输入框选择器。
- 不直接调用 `Dom.simulateClick`,只能走 adapter `switchSession()`。
- 不改 `PositionTracker.assign()`。
- 不改 `message_id` 合成规则。
- 不改 server batch 的采集权最终判定。
- 非目标页面 `auto_switch_session` 必须为 false。
- 单候选切换后必须能证明采集已经触发或完成,不能只等 3 秒。
- 每次切换前必须重新匹配 DOM 节点,不能复用跨切换缓存的 `dom_ref`。

## 12. 验收标准

### A. 静态验收

- `rg "sendReply|verifySent|simulateInput|textarea|contenteditable" plugin/runtime/assisted-collector.js` 无发送/输入相关实现。
- `assisted-collector.js` 不直接调用 `Dom.simulateClick`。
- `switchSession()` 关闭开关时返回 `auto-switch-disabled`。
- `npm run build:plugin -- --check` 通过。

### B. 本地模拟验收

- `auto_switch_session=false`:不切换。
- 非目标 URL:不切换。
- 人工事件触发后 1 秒内暂停。
- 候选超过 5 个时只处理 5 个。
- 单候选切换前会重新 detect 并重匹配 `dom_ref`。
- 切换后显式采集或停留超过 10 秒 debounce 窗口。
- block 时停止调度。

### C. 真机验收

- 抖音私信当前可见 3-5 个未读候选可被低频切换。
- 切换后现有采集链路能入库。
- 连续切换 3 个候选时,每个候选都有采集触发日志或入库证据。
- 人工操作立即暂停。
- 非采集负责人账号不自动切换。
- disabled 账号不自动切换。
- W20 account block 后不自动切换。
- W17 position/message_id/occurred_at 回归通过。

## 13. 分支与实施顺序

W21 代码实施顺序:

1. W20 merge 回 main。
2. 从最新 main 切 `w21-assisted-collector`。
3. 先实现静态闸门和状态机。
4. 再接候选调度。
5. 最后做真机验收。
