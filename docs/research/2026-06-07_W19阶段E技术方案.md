# W19 阶段E 技术方案 — 隔离收口 + 清库重采 + 发版 v0.5.0

> 版本: v1.0.0　日期: 2026-06-07　身份: DEV(CC1 出方案)　状态: **待 Chase 审 + 发版前 codex review**
> 性质: ★**不可逆硬关卡**(清库=永久销毁业务数据 / 发版=改生产)。依据: design §8 + DEPLOY.md + OPS prod-safety + W17/W19 迁移现状。
> ★执行纪律:本方案审过 → 逐步执行,**清库/发版前必须:备份离机验证可解压 + 破坏性三问 + Chase 在场**;发版前过 codex review。CC1 不擅自跑清库/部署命令。

## 0. 现状(已核实)
- prod `124.222.146.193`:/opt/chatsift,docker(chatsift-mysql + chatsift-server@127.0.0.1:3100),nginx(宝塔)admin.kongyuekeji.com,**VERSION=0.3.1**(W17 已并 main 未发版,故 prod 无 W17 也无 W19)。test:/opt/chatsift-test@3101,库 chatsift_test,全隔离。
- init 基线(fresh DB):`00_reused` + `v1-schema`(已含 W17 列) + `02_w19`(三表 + ALTER + role_code/role_scope/page_key + menus seed)。compose 挂 3 文件,check-version 计数一致。
- prod 是**已有库**:init 不重跑 → 须走 deploy 手动迁移。`deploy/w17_message_position.sql` 已有;**`deploy/w19_*.sql` 缺,需产出**。
- ★W19-B3 改了 conversation_id 口径 ⇒ message_id 全变 ⇒ 旧消息/会话对不上新 id ⇒ **必须清库重采**(与 W17 position 一次清,design §8 已定)。

## 1. 范围(E1–E5)
| 子步 | 内容 | 可逆性 |
|---|---|---|
| E1 | 隔离收口:dashboardController data_scope→user_type 对齐(Q5 遗留)+ grep 静态检查无裸 tenant | 可逆(代码) |
| E2 | prod schema 迁移:产出 `deploy/w19_tenant_asset.sql`;prod 应用 w17 + w19 迁移 | schema 只增,低险;改 prod |
| E3 | ★清库:TRUNCATE 业务数据(保留 RBAC/字典/租户/插件授权) | **不可逆** |
| E4 | ★发版 v0.5.0:版本三件套 + check-version + codex review + build + 部署 + 冒烟 | 改生产(可回滚) |
| E5 | 重采 + 隔离最终验收:插件 serverUrl→prod,真机重采,三类角色隔离真验 | — |

## 2. E1 隔离收口(可逆,先做)
- **dashboardController**(`/api/dashboard/stats`,平台首页基础统计):现用 `req.user.id` 当 tenant + `req.user.data_scope`(W19 前遗留)。改:平台方(internal/is_super)看平台级计数(不按 tenant);移除对 data_scope 的依赖。★阶段A 特意没动 data_scope 是怕 dashboard/plugin/logs 跨租户泄漏——本步**只动 dashboardController 自身**,不动 utils/data-scope.js 给 plugin/logs 的用法(那两个是 user 级授权,非租户业务,保持)。
- **grep 静态检查**(写进验收):全仓无 v1 业务路径裸用 `req.user.id` 当 tenant、无 controller 自拼 `tenant_id =`(scope helper 之外)。
- 验证:dashboard 页平台方正常出数、无 500;隔离断言复跑(A 的 7/7 + B 的闭环)。

## 3. E2 prod schema 迁移(改 prod,schema 只增)
- 产出 **`deploy/w19_tenant_asset.sql`**(给已有库:= `02_w19` 内容,CREATE IF NOT EXISTS + ALTER ADD + 全部 seed;对 prod v0.3.1 一次性可跑)。
- prod 迁移顺序(★备份后):① `deploy/w17_message_position.sql`(messages 加 position/segment_at + occurred_at 放松)② `deploy/w19_tenant_asset.sql`(三表 + 业务表加 service_account_id + users 加 tenant_id/user_type + roles role_code/role_scope + platform_pages page_key + menus/角色权限 seed + 首页去重)。
- ★顺序要点:w17 先(messages 结构),w19 后(依赖 platforms/platform_pages/users/业务表)。两者均"只增列/建表",对 v0.3.1 安全;seed 幂等。
- test 先行:同序在 test 跑一遍验证无报错,再上 prod。

## 4. E3 清库(★不可逆,Chase 在场)
- **范围**(design §8.1):TRUNCATE 业务数据 = `messages / conversations / leads / workorders / analysis_jobs / service_accounts / employee_service_account`(+ 可能的 message 衍生)。**保留**:users / roles / role_has_permissions / menus / platforms / platform_pages / tenants / plugins(授权) / tenant_llm_config。
- 前置硬要求(OPS 破坏性三问 + DEPLOY §2):① 全量 mysqldump chatsift → 压缩 → scp 离机 + COS,**验证可解压 + 记录行数/大小**;② 环境三确认(hostname/IP/库名);③ Chase 在场答完三问。
- 执行:`SET FOREIGN_KEY_CHECKS=0; TRUNCATE ...; SET FOREIGN_KEY_CHECKS=1;`(按 FK 顺序;service_accounts/employee_service_account 被业务表 FK 引用,先清引用方)。
- 回滚:误清 → 离机备份恢复(故备份是唯一安全网,必须先验证可解压)。

## 5. E4 发版 v0.5.0(★改生产,可回滚)
- 版本:`VERSION` 0.3.1→**0.5.0**(W19 大功能 minor;W17+W19 一起发);`sync-version.sh` 同步三件套;`CHANGELOG.md` + `PROJECT_STATUS.md` 记录;`check-version.sh` 必须过(SQL 文件数==compose 挂载数;当前都 3,一致)。
- ★**发版前 codex review**(Chase 要求):对 W17+W19 全量改动做一次 review,过了再部署。
- 部署(DEPLOY §3 简化,nginx 已指向 chatsift):① 本地 `npm run build:admin` 推 admin/dist ② `cd /opt/chatsift && git pull`(需先 push main)③ `docker compose -f deploy/docker-compose.prod.yml build && up -d` ④ E2 迁移已在库 ⑤ `nginx -t && reload`(已指 chatsift,通常不动)。
- test 先发:同步骤上 test(3101)冒烟 PASS 再上 prod。
- 冒烟(scripts/qa/smoke 或手测):登录三类角色 / mychat 首页 / 租户管理 / 客服账号分配 / 消息聚合客服下拉 / events batch 造数 → 归属+隔离正确。
- 回滚:不稳 → `git checkout v0.3.1` 重 build/up(库迁移只增,旧码兼容);数据兜底=E3 前备份。

## 6. E5 重采 + 隔离最终验收
- 插件 serverUrl 指 `https://admin.kongyuekeji.com`(DEPLOY 6.1 遗留项),真机重采 private-message(带 account_biz_id)。
- 三类角色真数据验收:internal 默认看不到 / 租户超管看本租户全部 / 客服只看分配账号(B2 自动绑定 + C3 手工分配);conversation_id 含 account_biz_id 跨账号不误并;position 0 重复(W17)。
- 写 `docs/reports/W19_stageE_acceptance.md`。

## 7. 待 Chase 拍板(Q#)
- **Q1 清库方式**:TRUNCATE 业务表(保留 RBAC/租户/字典/授权,本方案默认)✓?还是 DROP+re-init(更干净但丢 prod 现有 RBAC 定制/租户/插件授权,需重建)?(倾向 **TRUNCATE**)
- **Q2 test 先行**:E2 迁移 + E4 发版都先在 test 跑一遍再上 prod(本方案默认)?(倾向 **是**)
- **Q3 prod 现有业务数据**:prod v0.3.1 库里现有多少真实业务数据?清库前备份是硬要求;若 prod 当前几乎无真实数据,清库风险更低(仍须备份)。请确认 prod 数据现状。
- **Q4 发版节奏**:E1(可逆收口)审过我可先做;E2-E5(改 prod/不可逆)等你定时间窗 + 在场 + codex review。是否先做 E1?
- **Q5 codex review 范围**:发版前 codex review 覆盖 W17+W19 全量,还是仅 W19?(倾向 **W17+W19 一起**,因一次发版)

---

**未执行任何清库/部署。** 我能现在做的=E1(隔离收口,可逆)+ 产出 `deploy/w19_tenant_asset.sql`(迁移文件,不执行)。E2-E5 改 prod/不可逆,等你审方案 + 定 Q1-Q5 + 定时间窗(备份+在场+codex review)再逐步执行。
