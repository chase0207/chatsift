# 版本与编号现状核实

> 版本: v1.0　日期: 2026-06-04　身份: DEV（只读核实）
>
> 本文为只读盘点，记录核实当刻 chatsift 仓库的版本文件 / 脚本 / git tag 现状，供 AGENTS 版本章节定稿 + 编号规范使用。
> 核实分支：`w17-message-position`（== `main@7abe48f`）。

## 一、版本文件现状

| 文件 | 版本号 |
|---|---|
| `VERSION` | **0.3.1** |
| `server/package.json` | **0.3.1** |
| `admin/package.json` | **0.3.1** |
| `plugin/manifest.json` | **0.3.1** |
| `tools/dom-collector/manifest.json` | 1.0.0（独立工具，单独版本线） |

- 根目录**无** `dom-collector/`；chatsift 的 dom-collector 在 **`tools/dom-collector/`**（独立采集研究工具，codex 产出，自带 1.0.0 版本线）。
- 一致性：4 个产品版本文件（`VERSION` + server/admin/plugin）**全部 0.3.1，已一致，无差异**。`tools/dom-collector` 的 1.0.0 为独立工具版本，不并入产品版本线。

## 二、脚本现状

`scripts/` 现有：

```
backup-mysql.sh
bump-version.sh
check-version.sh        ✓
package-plugin.sh
release-prod.sh
sync-version.sh         ✓
w16_migrate.js
w16_task6_role_perms.sql
chaos/  qa/  smoke/      (子目录)
```

- `sync-version.sh`：**在**。
- `check-version.sh`：**在**。
- 二者均存在，**无需从 chat_rpa 移植**。版本三件套一致性由 `check-version.sh`（以 `VERSION` 为基准校验 server/admin/plugin）+ `bump-version.sh` / `sync-version.sh` 维护。

## 三、git tag 列表

`git tag` 输出（共 15 个）：

```
v0.2.0  v0.2.1  v0.2.2  v0.2.3  v0.2.4  v0.2.5  v0.2.6  v0.2.7  v0.2.8
v0.2.9  v0.2.10 v0.2.11 v0.2.12 v0.3.0  v0.3.1
```

按创建时间顺序：v0.2.0…v0.2.8（均 06-02）→ v0.2.9 → v0.3.0 → v0.2.10 → v0.2.11 → v0.2.12 → v0.3.1（v0.2.9 起均 06-03）。最新 = **v0.3.1**。所有 tag 已 push 到 origin。

## 四、tag ↔ W周次/功能 对应

| tag | 日期 | 内容（该版主要功能） | 关联 W / 来源 |
|---|---|---|---|
| v0.2.0 | 06-02 | 从 chat_rpa 初始化 + 采集插件骨架 + icons 首发 | W1–W4 基线（据内容推断，打包前累积） |
| v0.2.1 | 06-02 | 采集客服(agent)登录账号名 | W4（据 commit 内容推断） |
| v0.2.2 | 06-02 | 会话展示对齐抖音（分隔条采自平台 + 昵称按需展示） | W6（推断） |
| v0.2.3 | 06-02 | 按消息提取客服昵称 + 看记录标题显示用户昵称 | W6 / W12.6（推断） |
| v0.2.4 | 06-02 | admin 菜单分组重构 + 版本号动态切换 + 日志中心占位 | 无明确 W（功能点） |
| v0.2.5 | 06-02 | 只读消息聚合三栏页 + 隐藏日志中心 | **W13（commit 明确）** |
| v0.2.6 | 06-02 | 聚合页顶部多平台切换（平台→页面→客服）+ 纳入 dom-collector 调研 | **W13（commit 明确）** |
| v0.2.7 | 06-02 | RBAC 修复：非超管已授权子菜单不显示 + 权限保存 | 无明确 W（推断 W15 邻域） |
| v0.2.8 | 06-02 | RBAC 修复：启动刷新权限（改授权后 F5 仍无权限） | 无明确 W（推断 W15 邻域） |
| v0.2.9 | 06-03 | 插件：客户昵称误读为登录客服名（去 userInfo 兜底） | 无明确 W（昵称修复链） |
| v0.2.10 | 06-03 | server：customer_nickname 只取 inbound 客户消息 | 无明确 W（昵称修复链） |
| v0.2.11 | 06-03 | 工单列表/详情显示实时客户名 + 回填存量标题 | 无明确 W（昵称修复链） |
| v0.2.12 | 06-03 | 聚合条目展示最后一条消息 + 相对时间（微信式） | 无明确 W（昵称修复链收尾） |
| v0.3.0 | 06-03 | 平台/租户双入口拆分（admin + mychat）首发 | **W16（commit 明确）** |
| v0.3.1 | 06-03 | 登录域名限制（内部/外部按域名分入口） | **W16（commit 明确）** |

补充事实（编号规范要用）：

1. **tag 非按 W 周次 1:1**。v0.2.0–v0.2.8 全在 06-02 同日发（W1–W12 早期工作在打包前累积进 v0.2.0 基线后增量补丁）；只有 **W13（v0.2.5/v0.2.6）**、**W16（v0.3.0/v0.3.1）** 由 commit message 明确标注关联 W，其余仅能据 commit 内容推断、或为功能点/修复补丁，无明确 W 归属。
2. **v0.2.9–v0.2.12（昵称修复线）与 v0.3.0–v0.3.1（W16 线）曾是双 agent 并行的两条线**，时间上交错发版，已由 M3 合并进单一 `main`（merge `7b672d3`，main 树 == w16 树）。
3. 下一步：W17 建议 **v0.4.0**（minor）；今后单线推进——W 大功能 → minor（`0.x.0`）、补丁修复 → patch（`0.x.y`）；`tools/dom-collector` 走独立版本线不并入产品版本。
