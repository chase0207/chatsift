# 任务：v0.6.5 客户端本地采集状态隔离与冷启动续编

Owner: CC4
Role: DEV
Status: ready_for_review
Branch: hotfix/v0.6.5-client-state-isolation
Created: 2026-06-11
Plan-by: CC4 / 2026-06-11(仅技术方案,未写业务代码)

---

## 1. PM 指令

目标:
- 根治 v0.6.4 test 验收暴露的客户端本地采集状态不隔离问题。
- 解决 `seen` 跨租户/跨环境串用导致的历史消息 skip。
- 解决 `PositionTracker` 冷启动从 0 编号导致的 position/message_id 撞号。
- 保持 W17 的核心语义:有几条存几条,重复连发各自 position 不同,server 幂等仍成立。

背景:
- 详见 `docs/reports/v0.6.0-v0.6.3_发版时间线与采集失效根因分析.md` §5、§5A、§5B、§7。
- v0.6.4 的 `forgetSeen` 只解决采集权 rejected 后可重试,不解决本地态跨上下文污染。

范围:
- `seen` 本地状态按环境、租户、客服账号隔离。
- `PositionTracker` 本地状态按环境、租户、客服账号、会话隔离。
- 冷启动时从 server 查询当前会话已入库 `MAX(position)`,从 `max+1` 续编,不能从 0 乱编。
- 评估 queue/batch 是否需要同样隔离,并在技术方案中给出结论和证据。

不做:
- 不处理来客 `NULL-SA` 导致 agent 不可见问题。
- 不处理 tenant2 `collected:0` 的 DOM 变体问题。
- 不做 W22 工单中心 UI。
- 不做清库、删数据、生产部署。
- 不改发送/回复/输入能力。

文件边界:
- 允许评估/设计涉及:
  - `plugin/runtime/event-collector.js`
  - `plugin/runtime/position-tracker.js`
  - `plugin/runtime/legacy-collector.js`
  - `plugin/runtime/event-uploader.js`
  - `plugin/runtime/queue-manager.js`
  - `plugin/runtime/event-queue.js`
  - `server/src/controllers/v1/eventsController.js`
  - 相关 route 文件
  - `plugin/content.js` 构建产物
- 禁止改:
  - `VERSION`
  - `CHANGELOG.md`
  - `PROJECT_STATUS.md`
  - test/prod 配置和数据
  - W22 工单中心文件

---

## 2. 执行步骤

第一步只出技术方案,不写业务代码:

1. 按 AGENTS.md 做开工前置:声明身份、环境、文件边界,确认 `git status`。
2. 从干净 main 创建分支 `hotfix/v0.6.5-client-state-isolation`。
3. 阅读报告 §5、§5A、§5B、§7,核对代码中的 `seen`、`PositionTracker`、server 去重链路。
4. 在本任务单中补充技术方案,至少覆盖:
   - 本地态 namespace 设计。
   - 环境隔离使用 normalized serverUrl 或其 hash,不得只靠显示文案。
   - 是否包含 `tab_id`:持久采集态不得包含 `tab_id`;运行实例态可以包含。
   - tenant_id / account_biz_id / conversation_id 的获取方式。
   - 冷启动查询 `MAX(position)` 的 server 只读接口设计。
   - 旧 key 兼容/迁移策略。
   - queue/batch 是否隔离的结论。
   - 回归测试矩阵。
5. 技术方案写完后,状态改为 `ready_for_review`,停下等 PM/codex/用户 review。

代码阶段需等技术方案通过后再开始。

---

## 3. Stop Gates

遇到以下情况必须停下回报:

- 工作区不干净且存在无关改动。
- 需要改 `message_id` 合成规则。
- 需要改 server 去重键或 messages 表 schema。
- 需要新增/修改 SQL migration。
- 需要清 storage 作为正式修复方案。
- 需要 test/prod 操作。
- 需要改 `VERSION` / `CHANGELOG.md` / 发版记录。
- 发现会破坏 W17 “有几条存几条”语义。
- 发现会影响 W20 采集权闸门或 W21 自动切换开关。

---

## 4. 验收标准

- [ ] 重装插件或清 storage 后,已有历史会话新增消息不再 `duplicated` 吞掉。
- [ ] 租户1采过同一抖音会话后切租户2,历史消息不再被客户端 `seen` skip。
- [ ] test/prod/local 切换时,`seen` 和 `position` 不串环境。
- [ ] 同租户、同客服账号、同会话重复采集仍幂等。
- [ ] 采集权 rejected 后,v0.6.4 的 `forgetSeen` 重试能力仍生效。
- [ ] W21 自动切换触发的 `collectNow` 仍复用同一 position 规则。
- [ ] 无发送、回复、输入相关改动。
- [ ] `npm run build:plugin -- --check` 通过。
- [ ] server JS 语法检查通过。

---

## 5. 技术方案(CC4,plan-only)

> 阶段:仅技术方案,不写业务代码。以下含一个**对 PM 原始建议的修正**(§5.4):单纯 `max+1` 续编会把可见历史重复入库,必须配 **re-align**。

### 5.0 现状与根因(代码核对)
| 本地态 | 现 storage key | namespace 现状 | 问题 |
|---|---|---|---|
| `seen`(已采 message_id 集) | `chatsift_event_seen_ids`(`event-collector.js`) | **全局,零 namespace** | 跨会话/租户/环境全部串用 → 切租户重采同会话历史被 skip |
| `PositionTracker`(`{nextPos, seq}`) | `w17_pos_{conversationId}`(`position-tracker.js` STORAGE_PREFIX) | 仅 conversationId(含 account_biz_id,**不含 tenant/env**) | ① 同抖音账号被两个 chatsift 租户采 → 同 key 串号;② test/prod 串号;③ 冷启动 `seq` 空 → 从 `pos:0` 重编 → 撞库历史 position → 新消息 `duplicated` 吞 |
| `queue`(待传事件) | `chatsift_event_queue`(`event-queue.js`) | 全局 | 切 env/账号后旧事件会被发到新环境/新租户 |
- message_id = `syn_hash(conversationId|position)`(`dom-utils.js synthMessageId` position 路径);position 由 `legacy-collector.js:124-130` `PositionTracker.assign` 赋值并重写 message_id。server 去重键 = `(tenant_id, platform_message_id)`(`eventsController.js`)。
- 根因共性:**客户端持久采集态命名空间只到浏览器 profile**,不含 env/tenant。详见复盘报告 §5/§5A/§5B。

### 5.1 namespace 设计(持久采集态)
- **颗粒度**:`env` + `tenant_id` + `account_biz_id`(+ `conversation_id` 给 position)。
- **持久态不含 `tab_id`**(tab 级是运行实例态,属 `collect_instances` 心跳,本任务不动);env/tenant/account 唯一确定一份持久采集态。
- **seen key**:`chatsift_seen_{envHash}_{tenant}_{accountBizId}`(每"环境+租户+客服账号"一个 Set)。
- **PositionTracker key**:`w17pos_{envHash}_{tenant}_{conversationId}`(conversationId 已含 account_biz_id;再叠 env+tenant 防跨租户/跨环境串)。
- **缺失 = fail-closed SKIP(PM 拍板,不用公共兜底 key)**:`serverUrl / tenant_id / account_biz_id` **任一缺失 → 不得用 `t0/aNA/e0` 等公共兜底 key 继续采集**(公共 key 会让不同上下文串号)。处置见 §5.3 兜底流程:先尝试刷新 auth / 重读页面;仍缺失 → **本轮跳过采集:不写本地态(seen/position)、不入队、不上传**,下轮重试。

### 5.2 环境隔离:用 normalized serverUrl 的 hash(不靠显示文案)
- `envHash = Dom.simpleHash(normalize(cfg.serverUrl))`;`normalize` = 去尾斜杠 + 小写。**只读持久化的 `cfg.serverUrl`,不读内存 AUTO_ENV/badge 文案**(后者正是 v0.6.4 修过的"会误回 prod"的不可靠态)。
- 用 hash 而非原串:避免 key 含 `://`、`.`、过长;长度可控、稳定。

- **tenant_id**:`userInfo.tenant_id`(chrome.storage;`authController` 登录响应 `data.userInfo.tenant_id` 已返回、background.js 已存)。
- **account_biz_id**:URL `accountId`(`adapter._readAccountBizId`)/ `sessionInfo.accountBizId`。
- **serverUrl**:持久化的 `cfg.serverUrl`(§5.2)。
- **conversation_id**:adapter 合成 `douyin-private-{accountBizId}-{hash(accountBizId|nickname)}`(已含 account_biz_id),沿用不改。
- **★缺失兜底流程(PM 拍板,fail-closed,不用公共 key)**:每轮采集前组装 contextKey 时,若 `serverUrl / tenant_id / account_biz_id` 任一缺失:
  1. **先尝试恢复**:`tenant_id` 缺(旧 token/未登录态)→ 调 `/api/auth/verify` 取 userInfo 回填;`account_biz_id` 缺(URL 未就绪/页面未加载完)→ 重读页面(`_readAccountBizId` 重取);`serverUrl` 缺 → 重读 `cfg.serverUrl`。
  2. **仍缺失 → 本轮跳过该会话采集**:**不组装公共 key、不写 seen/position、不入队、不上传**,记 degrade 日志,下轮重试。
  - internal 用户 `tenant_id=NULL`:平台方不参与采集(W19 口径),按"缺失"处理 → 跳过(不采)。

### 5.4 ★冷启动续编:server 只读接口 + re-align(**修正 PM 的 max+1 方案**)
- **问题**:若冷启动只取 `MAX(position)` 并把当前可见消息从 `max+1` 重编 → 可见的"历史消息"拿到全新 position → message_id 变 → server 视为新消息 **重复入库**(违反 W17"有几条存几条/幂等",触 §3 stop gate)。**故必须 re-align**:让 PositionTracker 把可见 DOM 历史**对齐回它们已入库的旧 position**,只有真·新消息才续编 `max+1`。
- **新接口(只读)**:`GET /api/v1/conversations/position-state`
  - 参数:`platform`、`platform_page`、`conversation_id`(= 客户端 conversationId = `platform_conversation_id`)、`limit`(默认 ~30,覆盖一个 DOM 窗口)。
  - 鉴权:复用 `auth` + `perm('conversation:list')`;tenant 由 JWT(`scope()` 同口径),**只能查本租户**。
  - 逻辑:按 `(tenant_id, platform, platform_page, platform_conversation_id)` 找 conversation;返回 `{ found, max_position, recent: [...] }`。`recent` server 可**倒序取最近 limit 条**(`ORDER BY position DESC LIMIT K`);**纯 SELECT,不建账号/不写库/不改 lifecycle**(同 W20.1 只读口径)。
  - `recent` 元素:`{ direction, content_text, position }`(PM 拍板可返回内容;实现时若认为内容暴露过宽,可改为 server 返回与客户端 `keyOf` 等价的 key,见 §5.9-3)。
  - **★route 落点与顺序(PM 拍板)**:`server/src/routes/v1/conversations.js`(`conversation:list`),且**新路由 `GET /position-state` 必须注册在 `/:id` 和 `/:id/messages` 之前**,否则会被动态 `/:id` 吞掉(`position-state` 被当作 `:id`)。
- **客户端冷启动**(`PositionTracker.assign` 内,当 `seq` 为空时):
  1. 调接口拿 `{ found, max_position, recent }`;
  2. **`recent` 用前必须按 `position ASC` 重排**(server 倒序取最近 K,客户端重建 `seq` 前升序排序),保证 `findOffset` 序列方向与现状一致;逐条算 `keyOf(direction, content_text)` 重建 `seq = [{k, p}]`、`nextPos = max_position + 1`;
  3. 走原 `findOffset` 锚点窗口对齐当前 DOM → **命中的历史复用旧 position(幂等)、prepend 的更早消息/底部新消息按 `nextPos` 续编**;
  4. 之后逻辑与现状一致。
- **★退化/失败(PM 拍板,均不从 0、不冒险续编)**:
  - `found=false`(server 无此会话=全新会话)→ 空 `seq` 从 0(空库不撞,安全)。
  - `found=true` 但当前 DOM **无法与 server recent re-align**(`findOffset` 返回 null/歧义)→ **不得 degrade 续编、不得从 0** → **本轮跳过该会话采集 + 记日志**,等下轮重试。
  - 接口**网络失败/超时** → 同样**本轮跳过该会话采集 + degrade 日志**,下轮重试。

### 5.5 queue/batch 是否隔离 — 结论
- **结论:queue 不做持久 namespace 隔离,但必须在"上下文切换"时 flush。**
- **证据/理由**:`event-uploader.tick()` 上传时实时读 `cfg.serverUrl`+token;queue 里的事件不带目标环境/租户。一旦 env/账号切换,残留 queue 会被发往**新环境/新租户**(错配、可能误入库到错租户)。queue 是**短时上传缓冲**(上传即清),无需长期分租户保留。
- **方案(PM 拍板:以 content-script contextKey 变化为准,不能只依赖 START_PLATFORM)**:
  - **content script 侧维护 `lastContextKey = envHash + tenant_id + account_biz_id`**(持久态同口径,不含 tab_id)。每轮采集/上传前计算当前 `contextKey`;**检测到与 `lastContextKey` 真实变化 → 先 flush `chatsift_event_queue`,再把 seen/position 句柄切到新 namespace,然后更新 `lastContextKey`**。
  - 原因:`START_PLATFORM`/`login`/`logout` 是用户手动触发,会漏掉**静默切换**(如 `storage.onChanged` 改了 `cfg.serverUrl`/重登换租户/换客服账号 URL 但没重新点启动)。以 content-script 实测 contextKey 为唯一判据,手动触发仅作辅助。
  - 次选(更重、不推荐):queue 也加 namespace 前缀持久分租户保留。
- 注:queue flush 是丢弃**未上传**的缓冲,不丢已入库数据;丢弃的会在下次扫描重新采到(经 re-align 不重复)。

### 5.6 旧 key 兼容/迁移
- 旧 key:`chatsift_event_seen_ids`、`w17_pos_{conversationId}`、`chatsift_event_queue`。
- **策略:不迁移、一次性弃用**。理由:旧 key 本身就是"跨上下文污染源",迁移=把污染带进新命名空间。v0.6.5 新逻辑**只读写新 key**,不再读旧 key:
  - 旧 seen 弃用 → 最多让历史多几次 `duplicated` 上传(server 幂等,无害,0 重复入库)。
  - 旧 position 弃用 → 触发 §5.4 冷启动 re-align(已设计为幂等)。
  - 旧 queue 弃用 → 残留旧事件丢弃(下次重采)。
- 旧 key 的物理清理**可选、非必须**(不作为正式修复手段——§3 stop gate"禁止清 storage 作为修复");如做,仅一次性删旧 key,不删新 key。

### 5.7 对 W17 / W20 / W21 的影响(均不触 stop gate)
- **W17**:**不改** message_id 合成规则、不改 server 去重键、不改 messages schema。position 语义不变;re-align 保证"有几条存几条 + 重复连发各自 position 不同 + 跨采集幂等"。✓
- **W20**:采集权闸门/collect-permission 不动;新接口只读、tenant-scoped、不碰 SA/lifecycle。v0.6.4 `forgetSeen` 在 namespaced seen 上继续生效(改的是 key,不是逻辑)。✓
- **W21**:`collectNow` 仍走 `legacy collectMessageSession → PositionTracker.assign`(namespaced 后)→ position 规则一致;自动切换开关/人工互锁不受影响。✓

### 5.8 回归测试矩阵
| # | 场景 | 预期 |
|---|---|---|
| 1 | 重装/清 storage 后,已采会话新增消息 | 新消息 `accepted`(re-align:历史复用旧 pos→server duplicated 0 新增;新消息续编 max+1→accepted),不再被 duplicated 吞 |
| 2 | 租户1采过同抖音账号 → 切租户2采同会话 | namespace 不同 → seen/position 不串;租户2 历史正常采(经各自 re-align),不被 skip |
| 3 | test/prod/local 切换 | seen/position key 的 envHash 不同 → 不串环境 |
| 4 | 同租户同账号同会话重采 | 幂等,无重复入库 |
| 5 | rejected → 开采集权 → 重采 | v0.6.4 forgetSeen 在新 namespace seen 上仍生效,可补传 |
| 6 | W21 自动切换 collectNow | 复用同一 namespaced PositionTracker,position 规则一致 |
| 7 | 全新会话(接口 found=false) | 空 seq 从 0,正常采,无撞号 |
| 8 | 冷启动接口网络失败 | 本轮跳过该会话 + degrade 日志,不从 0 冒险撞号;恢复后下轮正常 |
| 9 | `found=true` 但 DOM 无法与 server recent re-align | 本轮跳过 + 日志;**不 degrade 续编、不从 0**;下轮重试 |
| 10 | `serverUrl/tenant/account_biz_id` 任一缺失 | 先刷新 auth/重读页面;仍缺 → 跳过本轮:不写 seen/position、不入队、不上传;不组装公共兜底 key |
| 11 | **静默切换**(storage.onChanged 改 cfg.serverUrl/重登换租户,未点启动) | content-script 测到 `lastContextKey` 变化 → flush queue + 切 namespace;无残留事件误发 |
| 12 | 切 env/账号/登出(手动) | 同样 flush;手动触发仅辅助,最终以 contextKey 变化为准 |
| 13 | 静态 | `npm run build:plugin -- --check` 通过;`server/src` `node -c` 通过;无发送/输入改动 |

### 5.9 PM 已拍板结论(已纳入上文方案)
1. **seen 颗粒度 = `env + tenant + account_biz_id`,不细到 conversation**(position 才到 conversation)。✓ 已落 §5.1。
2. **冷启动接口失败 / 无法 re-align → 跳过本轮采集,不从 0 编号**。✓ 已落 §5.4 退化。
3. **接口可返回 `direction + content_text + position`**;实现时若认为内容暴露过宽,可改为 server 返回与 `keyOf` 等价的 key(二选一,实现阶段定)。✓ 已落 §5.4。
4. **旧 key 不迁移、不物理清理,只弃用**(新逻辑只读写新 key,不读旧 key)。✓ 已落 §5.6。
5. **缺失(serverUrl/tenant/account 任一)→ 先刷新 auth/重读页面,仍缺则跳过本轮、不写本地态、不上传**;不得用公共兜底 key。✓ 已落 §5.1/§5.3。
6. **queue flush 以 content-script `lastContextKey` 实测变化为准**,不只依赖 START_PLATFORM。✓ 已落 §5.5。
7. **新只读路由置于 `/:id` 与 `/:id/messages` 之前**;**recent 用前按 `position ASC` 重排**。✓ 已落 §5.4。

### 5.10 涉及文件(实现阶段,**当前不改**)
- `plugin/runtime/event-collector.js`(seen key namespace + 句柄切换)
- `plugin/runtime/position-tracker.js`(key namespace + 冷启动 re-align:调接口、recent 升序重建 seq、退化跳过)
- `plugin/runtime/legacy-collector.js`(组装/维护 content-script `lastContextKey`=envHash+tenant+account;每轮前测 contextKey 变化 → flush queue + 切 seen/position namespace;缺失则跳过本轮;传上下文给 assign)
- `plugin/runtime/event-queue.js`(暴露 flush);辅助触发点 `background.js` login/logout/START_PLATFORM、`popup.js` cycleEnv(辅助,最终以 content-script contextKey 为准)
- tenant_id 缺失回填:复用 `/api/auth/verify`(已存在,返回 userInfo)
- `server/src/controllers/v1/conversationsController.js` + `server/src/routes/v1/conversations.js`(新 `GET /position-state` 只读接口,**注册在 `/:id` 之前**)
- `plugin/content.js`(构建产物)
- **不改**:`message_id` 规则、server 去重键、messages schema、VERSION/CHANGELOG/PROJECT_STATUS、test/prod。

---

## 6. 执行记录

由 CC4 填写:

- 开始时间:2026-06-11(方案阶段)
- 执行分支:`hotfix/v0.6.5-client-state-isolation`(从干净 main 切)
- commit:本次仅提交本任务单(§5 技术方案);**未写任何业务代码**
- 执行命令摘要:只读调研 `event-collector.js`/`position-tracker.js`/`event-queue.js`/`legacy-collector.js`/`authController.js`/adapter 的 storage key、tenant_id 来源、conversationId 合成、server 去重链路
- 检查结果:方案阶段无构建;待 review 通过后进入代码阶段再跑 build:plugin/node -c
- 异常:无;**对 PM 原 max+1 方案做了修正(§5.4 必须 re-align,否则历史重复入库违反 W17 幂等)**,列为 review 重点

---

## 7. 最终结果

由 CC4 填写:

- 状态: 完成 / 阻塞
- 产物:
- 未决项:
- 是否触碰禁止项:

---

## 8. PM 审核

由 PM/用户填写:

- 审核结论: ready_for_review(CC4 已按下方 5 必补 + 4 拍板修订 §5,详见 §5.9 对账;待复审)
- 需要补充:
  - 方案方向通过,但以下 5 点必须补入技术方案后才能进入代码阶段。
  - `tenant_id / account_biz_id / serverUrl` 任一缺失时,不得使用 `t0 / aNA / e0` 等公共兜底 key 继续采集。应先尝试刷新 auth/重读页面;仍缺失则跳过本轮采集,不写本地态、不上传。
  - `/api/v1/conversations/position-state` 新路由必须放在 `/:id` 和 `/:id/messages` 之前,避免被动态路由吞掉。
  - `recent` 可以由 server 倒序取最近 K 条,但返回给客户端或客户端使用前必须按 `position ASC` 重建 `seq`,保证 `findOffset` 的序列方向正确。
  - `found=true` 但当前 DOM 无法与 server recent re-align 时,不得 degrade 续编,不得从 0 编号。应跳过本轮采集并记录日志,等待下一轮重试。
  - queue flush 不能只依赖 popup/background 的 `START_PLATFORM`。内容脚本侧需维护 `lastContextKey = envHash + tenant_id + account_biz_id`;检测到真实 contextKey 变化时 flush queue,并切换 seen/position namespace。
- 拍板结论:
  - `seen` 颗粒度为 `env + tenant + account_biz_id`,不细到 conversation。
  - 冷启动接口失败时跳过本轮采集,不能从 0 编号。
  - 接口可返回 `direction + content_text + position`;若实现时认为内容暴露过宽,可改为 server 返回与 `keyOf` 等价的 key。
  - 旧 key 不迁移、不物理清理,只弃用。
- 下一步:
  - CC4 先按以上意见修改本任务单 §5 技术方案,不写业务代码。
  - 补完后把本节审核结论改回 `ready_for_review`,停下等待复审。
- CC4 修订记录(2026-06-11,plan-only,未写代码):
  - 必补①缺失=fail-closed SKIP(不用 t0/aNA/e0)→ §5.1 + §5.3 兜底流程(先刷新 auth/重读页面,仍缺则跳过本轮、不写本地态、不上传)。
  - 必补②新路由置于 `/:id`、`/:id/messages` 之前 → §5.4 route 落点。
  - 必补③recent 用前按 `position ASC` 重建 seq → §5.4 客户端步骤2。
  - 必补④`found=true` 无法 re-align → 跳过本轮,不 degrade、不从 0 → §5.4 退化。
  - 必补⑤queue flush 以 content-script `lastContextKey` 实测变化为准(不只 START_PLATFORM)→ §5.5 + §5.10。
  - 4 拍板(seen 颗粒度/失败跳过/接口返回内容或 keyOf/旧 key 只弃用)→ §5.9 全部对账纳入。
  - 测试矩阵新增缺失跳过/re-align 失败/静默切换 flush(§5.8 #9-12)。
