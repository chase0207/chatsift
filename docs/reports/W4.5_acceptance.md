---
文档: W4.5 验收报告
版本: v1.0.0
周次: W4.5
状态: Done
---

## 修复范围

- 补齐 `server/src/utils/platforms.js` 的 chat_rpa 历史工具函数: `normalizePlatformKey`, `getPlatformAliases`, `extractDetectHosts`
- 记录补迁到 `MIGRATED_FROM_CHAT_RPA.md`
- 保留插件平台定义兜底,并补注释说明这是服务端异常时的降级保险
- 未修改 `platformController.js` 路由和 controller 逻辑

## Fix Task 1: normalizePlatformKey 调用方排查

命令:

```bash
grep -rn "normalizePlatformKey" server/src/
```

结果:

```text
server/src/utils/platforms.js:1:function normalizePlatformKey(platform) {
server/src/utils/platforms.js:5:module.exports = { normalizePlatformKey }
server/src/controllers/deviceController.js:2:const { normalizePlatformKey } = require('../utils/platforms')
server/src/controllers/deviceController.js:4:const normalizePlatform = normalizePlatformKey
server/src/controllers/pluginController.js:3:const { normalizePlatformKey } = require('../utils/platforms')
server/src/controllers/pluginController.js:44:    const normalizedPlatform = normalizePlatformKey(platform)
server/src/controllers/pluginController.js:77:    if (platform   !== undefined) { fields.push('platform = ?');   values.push(normalizePlatformKey(platform)) }
server/src/controllers/platformController.js:2:const { normalizePlatformKey, extractDetectHosts } = require('../utils/platforms')
server/src/controllers/platformController.js:35:        platform_code: normalizePlatformKey(row.platform_key),
server/src/controllers/platformController.js:36:        runtime_key: normalizePlatformKey(row.platform_key),
```

判断:

- `platformController`:来自 chat_rpa 同源 controller,期望 `normalizePlatformKey` 做别名归一。
- `pluginController`:创建/更新插件授权平台,与 chat_rpa 同源;别名归一更符合授权平台存储语义。
- `deviceController.resolveUserGrant`:用于按平台查询授权,别名归一是期望行为;不依赖原样小写。

结论:未发现已验收代码依赖“原样小写”行为,按 v1.1.0 方案整文件补齐是安全路径。

## /api/platforms 验证

命令:

```bash
curl -i -H "Authorization: Bearer $TOKEN" http://127.0.0.1:3100/api/platforms
```

响应状态:

```http
HTTP/1.1 200 OK
```

抖音平台片段:

```json
{
  "code": 0,
  "douyin": {
    "platform_key": "douyin",
    "detect_hosts": [
      "im.jinritemai.com",
      "fxg.jinritemai.com",
      "buyin.jinritemai.com",
      "life.douyin.com",
      "im.douyin.com",
      "anchor.douyin.com"
    ],
    "pages": [
      {
        "page_name": "来客私信",
        "url": "https://life.douyin.com/cs/web/",
        "detect_hosts": ["life.douyin.com"]
      },
      {
        "page_name": "飞鸽",
        "url": "https://im.jinritemai.com",
        "detect_hosts": ["im.jinritemai.com"]
      },
      {
        "page_name": "抖音私信",
        "url": "https://life.douyin.com/cs/web/clue_private_message/chat/session",
        "detect_hosts": ["life.douyin.com"]
      }
    ]
  }
}
```

## W1 关键接口重测

### events/batch

命令:

```bash
curl -X POST http://127.0.0.1:3100/api/v1/events/batch \
  -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"events":[{"platform":"douyin","conversation_id":"w45_check","message_id":"w45_msg_1","direction":"inbound","content_type":"text","content_text":"测试","occurred_at":"2026-05-30T15:00:00+08:00"}]}'
```

响应:

```json
{"code":0,"message":"ok","data":{"accepted":1,"duplicated":0,"rejected":0}}
```

### 冒烟接口

```bash
curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:3100/api/v1/conversations
curl -H "Authorization: Bearer $TOKEN" http://127.0.0.1:3100/api/v1/workorders
```

结果:均返回 `code:0`。

## 插件兜底处置

保留 W4 添加的插件平台定义兜底。已在 `plugin/background.js` 注释标明:W4.5 已修复服务端 `/api/platforms` 500,该逻辑仅作为服务端异常时的降级保险。

## 语法检查

```bash
node --check server/src/utils/platforms.js
node --check plugin/background.js
```

结果:通过。

## Git Log

```bash
0c50921 fix(W4.5): restore missing platform utils (extractDetectHosts/getPlatformAliases)
7cf17a2 docs(W4): record real douyin acceptance results
f266b35 fix(W4): normalize stale local server url in uploader
386aad4 fix(W4): use chatsift local server port in plugin popup
fb6843e fix(W4): load adapter helpers after identity resolver
469fa75 fix(W4): actively start collector from popup
```
